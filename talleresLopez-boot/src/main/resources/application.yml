spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3307/talleresLopez-db
  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: validate
        format_sql: true